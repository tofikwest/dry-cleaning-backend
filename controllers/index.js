const users = require("./users");
const orders = require("./orders");
const auth = require("./auth");
const drycleaners = require("./drycleaners");
const services = require("./services");

module.exports = {
  users,
  drycleaners,
  orders,
  auth,
  services,
};
